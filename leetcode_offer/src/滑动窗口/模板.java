package 滑动窗口;

public class 模板 {
    //链表三兄弟
    //快慢指针操作链表
    //左右指针
    //滑动窗口

   /* int left = 0, right = 0;
    while (right < s.size()) {
    // 增大窗口
    window.add(s[right]);
    right++;

    while (window needs shrink) {
        // 缩小窗口
        window.remove(s[left]);
        left++;
    }*/
}

